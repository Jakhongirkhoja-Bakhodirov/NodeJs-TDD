const chai = require('chai');
const expect = chai.expect;

describe('foo test js' , () => {
    it('some test for foo.js file' , () => {
        expect(2).to.equal(2);
    });
    it('a pending test for foo.js file')
})