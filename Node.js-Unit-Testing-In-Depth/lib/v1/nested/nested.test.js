const chai = require('chai');
const expect = chai.expect;

describe('nested test js' , () => {
    it('some test for nested.js file' , () => {
        expect(2).to.equal(2);
    });
    it('a pending test for nested.js file')
})